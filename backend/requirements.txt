# Smart Traffic Navigation System -- backend dependencies
#
# The six modules and the UCS engine use only the Python standard library.
# Everything below is for the HTTP interface and the tests, which are optional:
# `python demo_cli.py` runs the entire system with no dependencies at all.

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
pytest>=7.4
httpx>=0.27          # used by fastapi.testclient
